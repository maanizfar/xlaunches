import React from "react";
import Events from "../components/events";

const EventsPage = () => {
  return (
    <section id="events">
      <Events />
    </section>
  );
};

export default EventsPage;
